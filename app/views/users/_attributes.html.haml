.row
  .col-md-6.col-md-offset-3
    %table.table.table-hover.without-borders
      %tbody
        = render partial: 'attribute', collection: @user.profile_attributes(my_page?).to_a
        - if my_page?
          %tr
            %td
            %td
              = submit_tag 'Обновить', class: ['btn', 'btn-primary']
