- if @post.creator != current_user
  - if @post.liked_by?(current_user)
    .alert.alert-success.text-center Вам понравилась данная статья
  - else
    .text-right
      = link_to like_post_path(@post), class: [:btn, 'btn-info'], method: :post do
        %span.glyphicon.glyphicon-thumbs-up
        Мне нравится данная статья
- else
  .text-right
    = link_to edit_post_path(@post), class: [:btn, 'btn-warning'] do
      %span.glyphicon.glyphicon-edit
      Редактировать

%h1.text-center= @post.title
%blockquote{ style: 'white-space: pre-wrap;' }
  = raw @post.body
  %footer.text-right
    автор
    = link_to @post.creator.any_name, @post.creator
